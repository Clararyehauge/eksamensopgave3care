---
// Henter parametrene fra Astro.props
const { htmlheading = "h2", size = "small", font = "sans", stylingclasses = "" } = Astro.props;
const Htmlheading = htmlheading;

const sizeClasses = {
  xlarge: "text-[45px] lg:text-[61px]", // H1
  large: "text-[29px] lg:text-[40px]", // H2
  medium: "text-[25px] lg:text-[35px]", // H3
  small: "text-[22px] lg:text-[31px]", //H4
  xsmall: "text-[17px] lg:text-[20px]", //H5

  body: "text-[13px] lg:text-[16px]",

  paragraphItalic: "text-[16px] lg:text-[22px] italic font-light",

  numbers: "text-[30px] lg:text-[45px]",
};

//Definer den styling (Tailwind classes), der gør sig gældende for de forskellige skrifttyper.
const fontClasses = {
  display: "font-display",
  sans: "font-sans",
  serif: "font-serif",
};
---

<Htmlheading class:list={[stylingclasses, sizeClasses[size], fontClasses[font]]}><slot /></Htmlheading>
